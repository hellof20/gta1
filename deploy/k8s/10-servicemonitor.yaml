apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: gta1-vllm
  namespace: gta1
  labels:
    app: gta1-vllm
spec:
  selector:
    matchLabels:
      app: gta1-vllm
  endpoints:
  - port: http
    interval: 15s
    path: /metrics
